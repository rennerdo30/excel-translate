services:
  addin-host:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: excel-translate-addin-host
    ports:
      - "3000:3000"
    environment:
      HOST: "0.0.0.0"
      PORT: "3000"
      LM_STUDIO_BASE_URL: "http://host.docker.internal:1234/v1"
